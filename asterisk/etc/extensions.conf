[general]
static=yes
writeprotect=no

[roip]
exten => _10XX,1,Dial(PJSIP/${EXTEN},20)
   same => n,Hangup()

; Grupo Broadcast
exten => 3000,1,Answer()
   same => n,Playback(beep)
   same => n,ConfBridge(3000,3000_user,3000,3000_menu)
   same => n,Hangup()


